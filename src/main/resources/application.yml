spring :
  application :
      name: project-crm
  datasource:
    url: jdbc:postgresql://localhost:5432/project_crm
    username: postgres
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update

